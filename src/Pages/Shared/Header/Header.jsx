import React from 'react';
import ThemeButton from '../../../components/Theme/ThemeButton';

const Header = () => {
    return (
        <nav>
           <ThemeButton/> 
        </nav>
    );
};

export default Header;